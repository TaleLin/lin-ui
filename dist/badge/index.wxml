<view class="l-badge" bindtap="handleTap">
  <slot/>
  <view class="l-badge-content l-class" wx:if="{{finalCount}}">{{finalCount}}</view>
  <view wx:if="{{content}}" class="l-badge-content">{{content}}</view>
  <view wx:if="{{dot}}" class="l-badge-dot  l-class"></view>
</view>  