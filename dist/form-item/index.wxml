<view class="form-item-container {{'label-' + labelPlacement}}">
    <view class="label {{required ? 'label-required': ''}}" style="min-width: {{labelWidth}}">{{label}}</view>
    <view class="content">
        <slot />
    </view>
</view>
<l-error-tip l-error-text-class="l-error-text l-error-text-class" errorText="{{errorText}}" wx:if="{{errorText}}"/>
